import React from 'react';

const FaceVerificationRoute = ({ children }) => {
  // Temporarily disable face verification
  // In the future, you can add your face verification logic here
  return <>{children}</>;
};

export default FaceVerificationRoute;